to test if the database is connectiong with the express server :

server.js is the server in package.json ->

   "scripts": {
    "start": "nodemon server.js"
}

  "dependencies": {
    "dotenv": "^17.2.2",
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "mongoose": "^8.18.2"
  },

rename tested.js to server.js

